export default class Todo {
    id: number
    title: string;
    status: boolean;
    description?: string

    constructor(
    ) {
        this.id;
        this.title = "";
        this.status = false;
        this.description = ""
    }
}